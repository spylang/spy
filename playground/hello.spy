# things to notice:
#   - functions require signature
#   - print works with only one argument for now
#   - after redshift, print is specialized
#   - with --redshift --no-pretty, you can see that lookups are resolved early

def add(x: i32, y: i32) -> i32:
    return x + y

def main() -> None:
    print("Hello from SPy 🥸")
    print(add(10, 20))
