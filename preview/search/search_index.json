{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#what-is-spy","title":"What is SPy?","text":"<p>TL;DR: SPy is a variant of Python specifically designed to be statically compilable while retaining a lot of the \"useful\" dynamic parts of Python.</p> <p>It consists of:</p> <ol> <li> <p>an interpreter (so that you can have the usual nice \"development      experience\" that you have in Python)</p> </li> <li> <p>a compiler (for speed)</p> </li> </ol> <p>The documentation is very scarce at the moment, but the best source to understand the ideas behind SPy are:</p> <ul> <li> <p>blog post Inside SPy, part 1: Motivations and Goals</p> </li> <li> <p>PyCon Italy 2025 talk: slides and recording.</p> </li> <li> <p>PyCon US 2024 talk: slides and recording.</p> </li> </ul> <p>Additional info can be found on:</p> <ul> <li>Antonio Cuni's blog</li> <li>A peek into a possible future of Python in the browser by \u0141ukasz Langa.</li> </ul> <p>This documentation is currently being written. Contributions are welcome!</p> <p>Source Code: https://github.com/spylang/spy</p>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#spy-source","title":"SPy Source","text":"<p>For guidelines on setting up your machine to build and contribute to SPy, see CONTRIBUTING.md</p>"},{"location":"contributing/#documentation","title":"Documentation","text":"<p>Cloning the Repo</p> <p>The first two steps below (forking and cloning the repo) are the same as for the steps for contributing to SPy's source code. You do not need a separate fork and clone for documentation.</p> <p>First, fork the  SPy repo, go to the project repo page and use the Fork button. This will create a copy of the repo, under your username (e.,g. <code>https://github.com/yourghname/spy</code>). (For more details on how to fork a repository see this guide.</p> <p>Next, clone the repo to your machine:</p> <pre><code>git clone git@github.com:your-user-name/spy.git\ncd spy\n</code></pre> <p>Create a virtual environment for your project, and install both the SPy dependencies and the docs-specific dependencies. Any package installer and virtual environment will work; here are a couple of options:</p> pipuv <pre><code>python -m venv .venv\nsource ./.venv/bin/activate # or similar for your flavor of shell\npython -m pip install .[docs]\n</code></pre> <pre><code>uv sync --group docs\n</code></pre> <p>Build a copy of the docs locally, and start a live server to view the current state of the docs:</p> pipuv <pre><code># with the virtual environment active\nmkdocs serve\n# open http://localhost:8000/mylocalcopy in a browser\n</code></pre> <pre><code>uv run mkdocs serve\n# open http://localhost:8000/mylocalcopy in a browser\n</code></pre> <p>Updating Docs</p> <p>Some changes - like adding or moving files, or changes to configuration in <code>mkdocs.yml</code>, will not be visible immediately in the server preview. To see them, stop the server (ctl/cmd + c) and restart the live server using the commands above.</p> <p>Create a new git branch to make changes on:</p> <pre><code>git checkout -b feature/your-change\n</code></pre> <p>At this point, you're ready to start writing and editing your contributions!</p> <p>Documentation Tools</p> <p>SPy uses mkdocs as its documentation builder with the material for mkdocs theme, and mike to manage docs versioning. See their pages for additional information on formatting and options.</p> <p>mkdocs' liveserver is more fully-featured than mike's, and is recommended for document previews. To view the versioned documents as they will be deployed, run <code>mike deploy testdocs &amp;&amp; mike serve</code>, then load localhost:8000/testdocs.</p> <p>Once you're happy with your changes, add them to your git branch and push them to GitHub.</p> <pre><code>  git add -A\n  git commit -m \"Describe your change\"\n  git push origin feature/your-change\n</code></pre> <p>Finally, open a Pull Request on the SPy GitHub page.</p>"},{"location":"llmem/","title":"Low-level memory model","text":"<p>SPy aims to be a \"two-level\" language, with a low-level, possibly unsafe core, upon which to build higher level abstractions which we expect end users to use.</p> <p>It might help to draw a parallel with CPython: the core of the interpreter and of many libraries is written in C, which is low level and inherently unsafe. The result is a safe high-level language which is what most people see and use.</p> <p>This document describes the low-level memory model of SPy.</p> <p>While reading this document, it is worth remembering that SPy has two main mode of execution, interpreted and compiled.</p> <p>The SPy compiler works by translating <code>*.spy</code> code into <code>*.c</code> code (after redshifting), which is then compiled by e.g. <code>gcc</code> or <code>clang</code>.  In the next sections, we will also explain how SPy types are translated into C.</p>"},{"location":"llmem/#save-vs-unsafe-code","title":"Save vs unsafe code","text":"<p>By default, SPy code is safe, meanging that:</p> <ol> <li> <p>memory and lifetimes are managed automatically</p> </li> <li> <p>you cannot corrupt memory or access memory which was already freed</p> </li> </ol> <p>However, SPy also offers the <code>unsafe</code> module, for writing low-level code and for specialized cases.  At the moment of writing, the <code>unsafe</code> module can be imported freely, but the plan is to allow unsafe code only in few specific and clearly labeled section of the program.</p>"},{"location":"llmem/#primitive-types","title":"Primitive types","text":"<p>At the core, we have primitive numeric types such as <code>i32</code>, <code>f32</code>, <code>f64</code>, etc. These are translated into their C equivalent <code>int32_t</code>, <code>float</code>, <code>double</code>, etc.</p> <p>Moreover, SPy defines the <code>int</code> and <code>float</code> aliases, which maps to <code>i32</code> and <code>f64</code> respectively. At the moment this is hardcoded, but eventually the precise mapping will depend on the target platform:</p> <sub>See on GitHub</sub> spy/vm/modules/builtins.py @ 37ee3e29<pre><code># add aliases for common types. For now we map:\n#   int -&gt; i32\n#   float -&gt; f64\n#\n# We might want to map int to different concrete types, depending on the\n# platform? Or maybe have some kind of \"configure step\"?\nBUILTINS.add(\"int\", BUILTINS.w_i32)\nBUILTINS.add(\"float\", BUILTINS.w_f64)\n</code></pre>"},{"location":"llmem/#stack-allocated-structs","title":"Stack-allocated structs","text":"<p>We can define C-like structs:</p> <pre><code>@struct\nclass Point:\n    x: int\n    y: int\n</code></pre> <p>Structs can be instantiated directly, and are immutable:</p> <pre><code>p = Point(1, 2)\nprint(p.x, p.y)\n\np.x = 3 # TypeError\n</code></pre> <p>Structs have inline storage:</p> <ol> <li> <p>if used as local variables, they are allocated \"on the stack\";</p> </li> <li> <p>if used as fields of a bigger struct, they are allocated \"inline\" the bigger      struct;</p> </li> <li> <p>they are passed by value, which means that passing around big structs can be      costly.</p> </li> </ol> <p>For example:</p> <pre><code>@struct\nclass Rect:\n    a: Point\n    b: Point\n\nassert sizeof(Point) == sizeof(int) * 2\nassert sizeof(Rect) == sizeof(int) * 4\n\nr = Rect(Point(1, 2), Point(3, 4))\n</code></pre> <p>The compiler translates them into plain C structs, something along these lines:</p> <pre><code>typedef struct {\n    int32_t x;\n    int32_t y;\n} Point;\n\ntypedef struct {\n    Point a;\n    Point b;\n} Rect;\n\nPoint p = {1, 2};\nRect r = {(Point){1, 2}, (Point){3, 4}};\n</code></pre> <p>Stack-allocated structs are always safe to use.</p>"},{"location":"llmem/#raw-and-gc-memory","title":"Raw and GC memory","text":"<p>The heap is conceptually divided into two main regions: raw memory and GC memory. The low-level manipulation of both areas of memory is unsafe.</p> <p>Raw memory is \"C style\":</p> <ul> <li> <p>memory is allocated with <code>raw_alloc[T]</code>; pointers are of type <code>raw_ptr[T]</code>;</p> </li> <li> <p>the memory must be explicitly released by calling <code>raw_free[T]</code> (NOT IMPLEMENTED     YET!)</p> </li> <li> <p>it is responsibility of the programmer to avoid use-after-free and out-of-bounds     access;</p> </li> <li> <p>once allocated, the address of the memory is non-movable and can be safely passed to     3rd party libraries</p> </li> </ul> <p>GC memory:</p> <ul> <li> <p>memory is allocated with <code>gc_alloc[T]</code>; pointers are of type <code>gc_ptr[T]</code>;</p> </li> <li> <p>the memory is automatically released by the GC when it's no longer needed;</p> </li> <li> <p>it is still responsibility of the programmer to avoid out-of-bounds access to     arrays;</p> </li> <li> <p>objects are potentially movable (depending on the GC strategy), so their address     might change;</p> </li> <li> <p>it is possible to get a temporary non-movable <code>raw_ptr</code> by \"pinning\" a <code>gc_ptr</code> (NOT     IMPLEMENTED YET!).</p> </li> </ul> <p>Warning</p> <p>GC is not implemented yet; currently <code>gc_alloc</code> is just an alias to <code>raw_alloc</code>, meaning that it leaks memory</p>"},{"location":"llmem/#heap-allocated-structs","title":"Heap-allocated structs","text":"<p>We can allocated structs \"on the heap\". This is a lower-level functionality which requires the use of <code>unsafe</code> functions; they can be allocated both in raw and GC memory:</p> <pre><code>from unsafe import raw_ptr, raw_alloc\n\np1: raw_ptr[Point] = raw_alloc[Point](1)\np1.x = 1\np1.y = 2\n</code></pre> <p>Contrarily to their stack-allocated counterparts, heap-allocated structs are mutable. you should think of heap-allocated structs as the basic building blog for all higher-level types.</p> <p>It might be helpful to draw again a parallel to CPython: in CPython, objects of type <code>tuple</code> and <code>str</code> are immutable, but under the hood they are implemented by mutable heap allocated structs written in C.</p>"},{"location":"llmem/#raw-allocation","title":"Raw allocation","text":"<p><code>raw_alloc[T](n)</code> allocates an array of <code>T</code> on the heap. To allocate a single element, you just pass <code>n = 1</code>. For convenience, if <code>T</code> is a struct you can access it's fields without having to use <code>[0]</code>, exactly as in C:</p> <pre><code>def test(p: raw_ptr[Point]) -&gt; None:\n    assert p.x == p[0].x\n    assert p.y == p[0].y\n</code></pre> <p>The low-level representation of pointers depends on the excecution mode.</p> <p>The interpreter keeps track of the address and the length of the allocated region, and checks for out-of-bounds access:</p> <sub>See on GitHub</sub> spy/vm/modules/unsafe/ptr.py @ 8a360bc1<pre><code>@UNSAFE.builtin_type(\"__base_ptr\")\nclass W_BasePtr(W_Object):\n    [...]\n    w_ptrtype: W_BasePtrType\n    addr: fixedint.Int32\n    length: fixedint.Int32  # how many items in the array\n</code></pre> <p>The same happens in debug compiled mode, where <code>raw_ptr[T]</code> is translated to a fat pointer. Finally, in release compiled mode, <code>raw_ptr[T]</code> is translated as a plain C pointer, and there is no out-of-bounds check:</p> <sub>See on GitHub</sub> spy/libspy/include/spy/unsafe.h @ 8a360bc1<pre><code>   typedef struct Ptr_T {\n       T *p;\n   #ifdef SPY_PTR_CHECKED\n       size_t length;\n   #endif\n   } Ptr_T;\n</code></pre>"},{"location":"llmem/#gc-allocation","title":"GC allocation","text":"<p>Not implemented yet</p> <p>See the very rough plan</p>"},{"location":"llmem/#raw-references-raw_reft","title":"Raw references: <code>raw_ref[T]</code>","text":"<p>Structs and pointers are loosely modeled against C, but there is a big semantic difference between Python and C that we need to take into account in order to provide an intuitive way to deal with structs.</p> <p>Consider the following example, using the <code>Rect</code> and <code>Point</code> structs defined above. It modifies a nested struct:</p> <pre><code>def test(r: raw_ptr[Rect]) -&gt; None:\n    r.a.x = 0\n</code></pre> <p>In Python (and thus SPy) the above expression decomposes to:</p> <pre><code>tmp = r.a\ntmp.x = 0\n</code></pre> <p>or, more explicitly:</p> <pre><code>tmp = getattr(r, \"a\")\nsetattr(tmp, \"x\", 0)\n</code></pre> <p>The naive implementation of <code>r.a</code> would be to return a copy of the <code>Point</code> but this means that <code>tmp.x</code> would modify the copy, not the original.</p> <p>To solve the problem, we return a reference instead:</p> <pre><code>tmp: raw_ref[Point] = r.a\ntmp.x = 0\n</code></pre> <p>Contrarily to pointers, references cannot be indexed and cannot be <code>NULL</code>. Moreover, a <code>raw_ref[T]</code> can be automatically converted into a <code>T</code>. E.g. consider the following:</p> <pre><code>def foo(r: raw_ptr[Rect]) -&gt; None:\n    r2: Rect = r   # ERROR: cannot convert raw_ptr[Rect] to Rect\n    p: Point = r.a  # works: r.a is raw_ref[Point], and it's converted to a Point\n</code></pre> <p>In the C backend, <code>raw_ref[T]</code> is implemented in the exact same way as <code>raw_ptr[T]</code>.</p>"},{"location":"api_reference/python_builtins/","title":"CPython-Like Builtins","text":""},{"location":"api_reference/python_builtins/#implemented-cpython-like-built-ins","title":"Implemented CPython-Like Built-ins","text":"<p>The following built-in functions work similarly to their equivalents in CPython; see the specific functions below for notes</p>"},{"location":"api_reference/python_builtins/#absobject","title":"abs(object)","text":"Currently only implemented for int32's or objects convertible to int32's. The <code>__abs__</code> attribute is not currently supported."},{"location":"api_reference/python_builtins/#breakpoint","title":"breakpoint()","text":"Drops the user into an interactive SPy debugging session via <code>spdb</code>, a <code>pdb-like</code> debugging interface for SPy."},{"location":"api_reference/python_builtins/#dictkeytype-valuetype","title":"dict[keytype, valuetype]()","text":"In SPy, <code>dict</code> must always be fully typed and used as <code>dict[keytype, valuetype]</code>., The syntax <code>dict[keytype, valuetype]()</code> can be used to create a new empty dict of the given types. The simpler syntax <code>d: dict[keytype, valuetype] = {}</code> can also be used. Unlike CPython, this does not (currently) accept an Iterable to create a new dict from. The implementation (in SPy) of <code>dict</code> can be viewed here."},{"location":"api_reference/python_builtins/#dirobject","title":"dir(object)","text":"Returns a list of object\u2019s attributes\u2019 names, the names of its class\u2019s attributes, and recursively of the attributes of its class\u2019s base classes. <code>dir(type)</code> is not currently implemented. The no-argument form of <code>dir()</code> (i.e. print local variables) is not currently implemented. Custom <code>__dir__</code> methods on objects are not currently supported."},{"location":"api_reference/python_builtins/#floatobject","title":"float(object)","text":"Converts <code>object</code> to a float if able. <code>float</code> is an alias for the <code>f64</code> type."},{"location":"api_reference/python_builtins/#getattrobj-name-str","title":"getattr(obj, name: str)","text":"Return the value of the named attribute of object. <code>attr</code> must be blue"},{"location":"api_reference/python_builtins/#hashobject","title":"hash(object)","text":"Currently implemented for types: <code>i8</code>,<code>i32</code>, <code>u8</code>, <code>bool</code>, <code>str</code>. By default, instances of SPy structs are not hashable. As a planned future feature, structs will have auto-generated <code>__hash__</code> by default, but this is awaiting implementation. Currently, users can implement the <code>__hash__</code> function to permit hashing."},{"location":"api_reference/python_builtins/#intobject","title":"int(object)","text":"Converts <code>object</code> to an int if able. Works for number types, as well as strings. The <code>int</code> type is currently an alias to <code>i32</code>. In the future, <code>int</code> will alias preferred individual types for specific platforms, but currently it is always <code>i32</code>."},{"location":"api_reference/python_builtins/#lenobject","title":"len(object)","text":"Return the length (the number of items) in a container"},{"location":"api_reference/python_builtins/#listtype","title":"list[type]()","text":"The syntax <code>list[type]()</code> can be used to create a new empty list of the given type. The simpler syntax <code>l: list[membertype] = []</code> can also be used. Unlike CPython, this does not (currently) accept an Iterable to create a new list from.  The implementation (in SPy) of <code>list</code> can be viewed here."},{"location":"api_reference/python_builtins/#maxx-i32-y-i32","title":"max(x: i32, y: i32)","text":"Currently only implemented for int32's or objects convertible to int32's."},{"location":"api_reference/python_builtins/#minx-i32-y-i32","title":"min(x: i32, y: i32)","text":"Currently only implemented for int32's or objects convertible to int32's."},{"location":"api_reference/python_builtins/#object","title":"object","text":"<code>object</code> is implemented as a type, and can be used as a parameter or return type. \"Plain\" objects (i.e. <code>x = object()</code>) are not supported."},{"location":"api_reference/python_builtins/#printobj","title":"print(obj)","text":"The print function is currently not variadic, in the sense that it only accepts a single argument. The built-in types are special-cased, and SPy can always print blue objects by pre-computing their string representation"},{"location":"api_reference/python_builtins/#rangestop","title":"range(stop) range(start, stop, step)","text":"<p>Creates an iterable set of indices between <code>start</code> and <code>stop</code>, jumping over <code>step</code> indices between each.</p> <p>The implementation (in SPy) of <code>range</code> can be viewed here.</p>"},{"location":"api_reference/python_builtins/#reprobject","title":"repr(object)","text":"Returns string containing a printable representation of an object."},{"location":"api_reference/python_builtins/#setattrobject-name-str-value-obj","title":"setattr(object, name: str, value: obj)","text":"Assigns <code>value</code> to the attribute of <code>object</code> named by <code>name</code>. <code>attr</code> must be blue."},{"location":"api_reference/python_builtins/#slicestop","title":"slice(stop)slice(start, stop, step=None)","text":"<p>Return a slice object representing the items reached when iterating over range(start, stop, step). The start and step arguments default to None.</p>"},{"location":"api_reference/python_builtins/#strobject","title":"str(object)","text":"Returns a string version of the object. Selecting an encoding is not currently implemented."},{"location":"api_reference/python_builtins/#tuple","title":"tuple()","text":"The syntax <code>tuple[t1, t2, ...](val1, val2 ...)</code> can be used to create a new tuple, with <code>t1</code> as the type of <code>val1</code>, etc. unlike CPython, this does not (currently) accept an Iterable to create a new tuple from.  The implementation (in SPy) of <code>tuple</code> can be viewed here."},{"location":"api_reference/python_builtins/#typeobject","title":"type(object)","text":"Returns the type (i.e. the dynamic type at runtime) of an object"},{"location":"api_reference/python_builtins/#not-implemented-cpython-built-ins","title":"Not-Implemented CPython Built-ins","text":"<p>The following CPython built-ins are not currently implemented in SPy. Each category has a brief note about the current state of that category of object or function - some require additional internal mechanics, others are simply lower priority that other facets of the language to this point.</p>"},{"location":"api_reference/python_builtins/#async","title":"Async","text":"SPy does not currently have an async story. <p>aiter(), anext()</p>"},{"location":"api_reference/python_builtins/#iterables-and-iterators","title":"Iterables and Iterators","text":"<p>Iterables and collections are very much an area of active developmen; as their API solidifies, these types of builtins should become more straightforward to implement.</p> Generators are not currently supported in SPy. <p>all(), any(), enumerate(), filter(), iter(), map(), next(), reversed(), sorted(), sum(), zip()</p>"},{"location":"api_reference/python_builtins/#math","title":"Math","text":"Number types beyond int and float are in active development; some of the math functions below are also in active development. <p>divmod(), bin(), hex(), oct(), pow(), round()</p>"},{"location":"api_reference/python_builtins/#function-types-introspection-and-metaprogramming","title":"Function Types, Introspection and Metaprogramming","text":"The internals of SPy are significantly different from CPython; as such, the road to (and need for) some of these built-ins is less straightforward. Some are also waiting on internal details to solidify prior to implementation. <p>callable(), classmethod(), compile(), delattr(), eval(), exec(), globals(), hasattr(), help(), id(), isinstance(), issubclass(), locals(), property(), super(), vars(). __import__()</p>"},{"location":"api_reference/python_builtins/#type-conversion","title":"Type Conversion","text":"Many of these types are not implemented yet; others are in active development. <p>ascii(), bool(), bytearray(), bytes(), chr(), complex(), format(), frozenset(), memoryview(), ord() set()</p>"},{"location":"api_reference/python_builtins/#io","title":"I/O","text":"The I/O story is currently a high priority and is in active development. <p>input(), open()</p>"}]}